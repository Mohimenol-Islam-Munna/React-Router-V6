import React from "react";

const Home = () => {
  return (
    <div
      style={{
        width: "50%",
        border: "1px solid salmon",
        padding: "20px 10px",
        margin: "20px auto",
        borderRadius: "10px",
      }}
    >
      Home component
    </div>
  );
};

export default Home;
